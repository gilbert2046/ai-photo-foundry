# Security

Never commit API keys, `.env` files, generated images, prompt history, usage ledgers, or local application data.

If a credential is accidentally committed, revoke it at the provider immediately, create a replacement, and remove the secret from the full Git history before publishing.
